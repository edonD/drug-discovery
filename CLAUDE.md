# Drug Discovery Agent

Read `program.md` first — it is the controlling document.

- Be creative about molecule generation
- Be strict about evaluation — every number must come from actually running the tools
- Keep only molecules that pass all specs
- Log everything in `molecules.json`, best hits in `best_molecules.json`
- Update `README.md` after every batch with top hits, SAR analysis, and plots
- Commit and push keepers
- Continue autonomously until manually stopped
